// Preload runs before the web app with access to a controlled bridge.
// Currently a no-op — reserved for future native integrations (file dialogs,
// auto-update, etc.) exposed safely via contextBridge.
